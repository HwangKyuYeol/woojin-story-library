const $=(q)=>document.querySelector(q); const $$=(q)=>[...document.querySelectorAll(q)];
let current=0; const pages=$$('.page');
function show(i){ if(!pages.length)return; const old=current; current=Math.max(0,Math.min(i,pages.length-1)); pages.forEach((p,idx)=>{p.classList.remove('active','leaving'); if(idx===current)p.classList.add('active')}); const c=$('#counter'); if(c)c.textContent=`${current+1} / ${pages.length}`; if(old!==current) sprinkle(); }
function next(){show(current+1)} function prev(){show(current-1)}
function sprinkle(){ for(let i=0;i<12;i++){ const s=document.createElement('span'); s.className='petal'; s.textContent=['🌼','🌸','✨','🍃'][Math.floor(Math.random()*4)]; s.style.left=Math.random()*100+'vw'; s.style.animationDuration=(2+Math.random()*2.5)+'s'; s.style.fontSize=(16+Math.random()*16)+'px'; document.body.appendChild(s); setTimeout(()=>s.remove(),5000)}}
document.addEventListener('DOMContentLoaded',()=>{show(0); $('#next')?.addEventListener('click',next); $('#prev')?.addEventListener('click',prev); $('.tap-zone.right')?.addEventListener('click',next); $('.tap-zone.left')?.addEventListener('click',prev); document.addEventListener('keydown',e=>{if(e.key==='ArrowRight')next(); if(e.key==='ArrowLeft')prev();}); let sx=0; document.addEventListener('touchstart',e=>sx=e.touches[0].clientX,{passive:true}); document.addEventListener('touchend',e=>{let dx=e.changedTouches[0].clientX-sx; if(Math.abs(dx)>40) dx<0?next():prev();},{passive:true});});
